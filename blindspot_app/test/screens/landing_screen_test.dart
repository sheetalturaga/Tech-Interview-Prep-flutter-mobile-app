import 'package:blindspot_app/screens/about_screen.dart';
import 'package:blindspot_app/screens/home_screen.dart';
import 'package:blindspot_app/screens/landing_screen.dart';
import 'package:blindspot_app/screens/login_screen.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:get/get.dart';

// void main() {
//   testWidgets('Landing screen Wedgets test', (WidgetTester tester) async {
//     // Build the widget tree
//     await tester.pumpWidget(const GetMaterialApp(
//       home: LandingScreen(),
//     ));

    // // Find the app bar
    // final appBarFinder = find.byType(AppBar);

    // // Find the login button
    // final loginButtonFinder = find.widgetWithText(ElevatedButton, 'Login');

    // // Find the about button
    // final aboutButtonFinder = find.widgetWithText(ElevatedButton, 'About');

    // // Find the arrow button
    // final arrowButtonFinder = find.byType(ArrowButton);

    // // Find the copyright text
    // final copyrightTextFinder =
    //     find.text('Copyright BlindSpot All Rights Reversed');

    // // Verify that the app bar is displayed
    // expect(appBarFinder, findsOneWidget);

    // // Verify that the login button is displayed
    // expect(loginButtonFinder, findsOneWidget);

    // // Verify that the about button is displayed
    // expect(aboutButtonFinder, findsOneWidget);

    // // Verify that the arrow button is displayed
    // expect(arrowButtonFinder, findsOneWidget);

    // // Verify that the copyright text is displayed
    // expect(
    //   copyrightTextFinder,
    //   findsOneWidget,
    //   reason: 'Expected the copyright text to be displayed',
    // );

    // // Tap the login button and verify that it navigates to the login screen
    // await tester.tap(loginButtonFinder);
    // await tester.pumpAndSettle();
    // expect(find.byType(LoginScreen), findsOneWidget);

    // // Tap the about button and verify that it navigates to the about screen
    // await tester.tap(aboutButtonFinder);
    // await tester.pumpAndSettle();
    // expect(find.byType(AboutScreen), findsOneWidget);

    // // Tap the arrow button and verify that it navigates to the home screen
    // await tester.tap(arrowButtonFinder);
    // await tester.pumpAndSettle();
    // expect(Get.currentRoute, HomeScreen.routeName);
//   });
// }
